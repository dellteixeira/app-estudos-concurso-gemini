(function(global){
'use strict';
const $=id=>document.getElementById(id);const core=()=>global.PdfStudyCore;const ann=()=>global.PdfStudyAnnotations;
let state={doc:null,pdf:null,page:1,total:0,scale:1.25,annotations:[],bookmarks:[],selected:null,openedAt:0,renderToken:0};
function currentContest(){return document.getElementById('concursoSelect')?.value||global.getLastStudiedConcurso?.()||'Concurso Geral'}
function currentLink(){const links=state.doc?.links||[];return state.doc?.activeLink||links.find(l=>l.concurso===currentContest())||links[0]||null}
function setStatus(t){const e=$('pdfReaderStatus');if(e)e.textContent=t||''}
function clearSelection(){state.selected=null;$('pdfReaderSelectionBar')?.classList.remove('show');try{window.getSelection()?.removeAllRanges()}catch(_){}}
function getSelectedGeometry(){const sel=window.getSelection();if(!sel||sel.isCollapsed||!sel.rangeCount)return null;const text=sel.toString().trim();if(!text)return null;const range=sel.getRangeAt(0);const node=range.commonAncestorContainer.nodeType===1?range.commonAncestorContainer:range.commonAncestorContainer.parentElement;const pageEl=node?.closest?.('.pdf-reader-page');if(!pageEl)return null;const box=pageEl.getBoundingClientRect();const rects=[...range.getClientRects()].filter(r=>r.width>1&&r.height>1).map(r=>({x:(r.left-box.left)/box.width,y:(r.top-box.top)/box.height,w:r.width/box.width,h:r.height/box.height}));if(!rects.length)return null;return {page:Number(pageEl.dataset.page),text:text.slice(0,20000),rects}}
function updateSelection(){const g=getSelectedGeometry();if(!g)return;state.selected=g;const bar=$('pdfReaderSelectionBar');if(bar){$('pdfReaderSelectionText').textContent=g.text.length>90?g.text.slice(0,90)+'…':g.text;bar.classList.add('show')}}
async function saveAnnotation(type,noteText=''){if(!state.selected)return alert('Selecione um trecho do PDF primeiro.');try{const color=type==='underline'?'#38bdf8':type==='note'?'#c084fc':'#fde047';const a=await ann().create({pdfId:state.doc.id,pageNumber:state.selected.page,type,selectedText:state.selected.text,noteText,color,rects:state.selected.rects});state.annotations.push(a);clearSelection();await renderAnnotations();renderSideList();setStatus('Marcação salva on-line.')}catch(e){handle(e)}}
function promptNote(){if(!state.selected)return alert('Selecione um trecho do PDF primeiro.');const note=prompt('Anotação para o trecho selecionado:','');if(note!==null)saveAnnotation('note',note)}
async function createFlashcard(){if(!state.selected)return alert('Selecione um trecho do PDF primeiro.');const link=currentLink();if(!link)return alert('Vincule o PDF a um concurso/matéria antes de criar flashcard.');const pergunta=prompt('Pergunta do flashcard:','O que este trecho estabelece?');if(!pergunta)return;try{if(typeof global.addPdfStudyFlashcard!=='function')throw new Error('Integração com flashcards indisponível.');await global.addPdfStudyFlashcard({materia:link.materia,assunto:link.assunto,pergunta,resposta:state.selected.text,sourcePdfId:state.doc.id,sourcePage:state.selected.page});clearSelection();setStatus('Flashcard criado e sincronizado com o concurso atual.')}catch(e){handle(e)}}
async function exportToNotes(){const link=currentLink();if(!link)return alert('Vincule este PDF ao concurso atual antes de exportar para Anotações.');if(!state.annotations.length)return alert('Ainda não existem marcações para exportar.');const blocks=state.annotations.map(a=>`Página ${a.page_number}\n${a.annotation_type==='highlight'?'GRIFO':a.annotation_type==='underline'?'SUBLINHADO':'NOTA'}: ${a.selected_text||''}${a.note_text?`\nMinha anotação: ${a.note_text}`:''}`).join('\n\n');try{if(typeof global.addPdfStudyNote!=='function')throw new Error('Integração com Anotações indisponível.');await global.addPdfStudyNote({materia:link.materia,assunto:link.assunto,titulo:`Anotações — ${state.doc.title}`,conteudoTexto:`PDF: ${state.doc.title}\n\n${blocks}`});setStatus('Marcações exportadas para Anotações.')}catch(e){handle(e)}}
function downloadMarkdown(){if(!state.annotations.length)return alert('Ainda não existem marcações.');const txt=`# ${state.doc.title}\n\n`+state.annotations.map(a=>`## Página ${a.page_number}\n\n**${a.annotation_type}** — ${a.selected_text||''}${a.note_text?`\n\n> ${a.note_text}`:''}`).join('\n\n');const blob=new Blob([txt],{type:'text/markdown;charset=utf-8'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=(state.doc.title||'anotacoes').replace(/[^a-z0-9_-]+/gi,'_')+'.md';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000)}
async function open(doc){if(!doc?.id)return;try{if(!global.pdfjsLib)throw new Error('PDF.js não carregado.');pdfjsLib.GlobalWorkerOptions.workerSrc='./vendor/pdf.worker.min.js';state={doc,pdf:null,page:Number(doc.progress?.current_page||1),total:0,scale:window.innerWidth<700?1:1.25,annotations:[],bookmarks:[],selected:null,openedAt:Date.now(),renderToken:0};$('pdfReaderOverlay').classList.add('open');document.body.classList.add('pdf-reader-open');$('pdfReaderTitle').textContent=doc.title||doc.original_file_name||'PDF';setStatus('Carregando PDF…');const blob=await global.PdfStudyLibrary.downloadBlob(doc);const data=new Uint8Array(await blob.arrayBuffer());state.pdf=await pdfjsLib.getDocument({data}).promise;state.total=state.pdf.numPages;state.page=Math.min(Math.max(1,state.page),state.total);$('pdfReaderTotalPages').textContent=String(state.total);$('pdfReaderPageInput').max=String(state.total);state.annotations=await ann().list(doc.id);state.bookmarks=await ann().bookmarks(doc.id);await global.PdfStudyLibrary.updatePageCount?.(doc.id,state.total);renderSideList();await renderPage();setStatus('Selecione um trecho para grifar, sublinhar, anotar ou criar flashcard.')}catch(e){handle(e);close()}}
async function close(){if(state.doc){await saveProgress(true).catch(()=>{})}$('pdfReaderOverlay')?.classList.remove('open');document.body.classList.remove('pdf-reader-open');clearSelection();state={doc:null,pdf:null,page:1,total:0,scale:1.25,annotations:[],bookmarks:[],selected:null,openedAt:0,renderToken:0};global.PdfStudyLibraryUI?.refresh?.()}
async function renderPage(){if(!state.pdf)return;const token=++state.renderToken;const page=await state.pdf.getPage(state.page);if(token!==state.renderToken)return;const viewport=page.getViewport({scale:state.scale});const host=$('pdfReaderPageHost');host.innerHTML='';const pageEl=document.createElement('div');pageEl.className='pdf-reader-page';pageEl.dataset.page=state.page;pageEl.style.width=viewport.width+'px';pageEl.style.height=viewport.height+'px';const canvas=document.createElement('canvas');canvas.width=Math.floor(viewport.width*devicePixelRatio);canvas.height=Math.floor(viewport.height*devicePixelRatio);canvas.style.width=viewport.width+'px';canvas.style.height=viewport.height+'px';const ctx=canvas.getContext('2d');const textLayer=document.createElement('div');textLayer.className='pdf-reader-text-layer';const markLayer=document.createElement('div');markLayer.className='pdf-reader-mark-layer';pageEl.append(canvas,textLayer,markLayer);host.appendChild(pageEl);await page.render({canvasContext:ctx,viewport,transform:devicePixelRatio!==1?[devicePixelRatio,0,0,devicePixelRatio,0,0]:null}).promise;const text=await page.getTextContent();await pdfjsLib.renderTextLayer({textContentSource:text,container:textLayer,viewport,textDivs:[]}).promise;pageEl.addEventListener('mouseup',()=>setTimeout(updateSelection,0));pageEl.addEventListener('touchend',()=>setTimeout(updateSelection,50));$('pdfReaderPageInput').value=String(state.page);updateBookmarkButton();await renderAnnotations();renderSideList();saveProgress(false).catch(()=>{})}
async function renderAnnotations(){const layer=$('pdfReaderPageHost')?.querySelector('.pdf-reader-mark-layer');const pageEl=$('pdfReaderPageHost')?.querySelector('.pdf-reader-page');if(!layer||!pageEl)return;layer.innerHTML='';for(const a of state.annotations.filter(x=>Number(x.page_number)===state.page)){for(const r of Array.isArray(a.rects)?a.rects:[]){const el=document.createElement('button');el.type='button';el.className=`pdf-reader-mark ${a.annotation_type}`;el.style.left=(r.x*100)+'%';el.style.top=(r.y*100)+'%';el.style.width=(r.w*100)+'%';el.style.height=(r.h*100)+'%';el.style.setProperty('--mark-color',a.color||'#fde047');el.title=a.note_text||a.selected_text||'Marcação';el.onclick=e=>{e.stopPropagation();openAnnotation(a.id)};layer.appendChild(el)}}}
function renderSideList(){const box=$('pdfReaderAnnotationsList');if(!box)return;const items=[...state.annotations].sort((a,b)=>a.page_number-b.page_number||new Date(a.created_at)-new Date(b.created_at));if(!items.length){box.innerHTML='<p class="pdf-reader-empty-side">Selecione um trecho para criar sua primeira marcação.</p>';return}box.innerHTML=items.map(a=>`<article class="pdf-reader-side-item" data-id="${a.id}"><button type="button" class="pdf-reader-side-main" onclick="PdfStudyReader.goToAnnotation('${a.id}')"><span>Pág. ${a.page_number} · ${a.annotation_type==='highlight'?'Grifo':a.annotation_type==='underline'?'Sublinhado':'Nota'}</span><strong>${esc(a.selected_text||a.note_text||'')}</strong>${a.note_text?`<em>${esc(a.note_text)}</em>`:''}</button><button class="pdf-reader-delete-mark" onclick="PdfStudyReader.deleteAnnotation('${a.id}')" title="Excluir">×</button></article>`).join('')}
function esc(v){return String(v||'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]))}
async function openAnnotation(id){const a=state.annotations.find(x=>x.id===id);if(!a)return;if(a.annotation_type==='note'){const n=prompt('Editar anotação:',a.note_text||'');if(n!==null){const updated=await ann().update(id,{note_text:n});state.annotations=state.annotations.map(x=>x.id===id?updated:x);await renderAnnotations();renderSideList()}}else{if(confirm(`${a.selected_text||'Marcação'}\n\nExcluir esta marcação?`))await deleteAnnotation(id)}}
async function deleteAnnotation(id){try{await ann().remove(id);state.annotations=state.annotations.filter(x=>x.id!==id);await renderAnnotations();renderSideList();setStatus('Marcação excluída.')}catch(e){handle(e)}}
async function goToAnnotation(id){const a=state.annotations.find(x=>x.id===id);if(!a)return;state.page=Number(a.page_number);await renderPage()}
async function prev(){if(state.page>1){state.page--;await renderPage()}}
async function next(){if(state.page<state.total){state.page++;await renderPage()}}
async function goto(v){const n=Math.min(state.total,Math.max(1,Number(v)||1));if(n!==state.page){state.page=n;await renderPage()}}
async function zoom(delta){state.scale=Math.min(2.5,Math.max(.65,state.scale+delta));$('pdfReaderZoomLabel').textContent=Math.round(state.scale*100)+'%';await renderPage()}
async function fitWidth(){const page=await state.pdf.getPage(state.page);const base=page.getViewport({scale:1});const available=Math.max(280,$('pdfReaderCanvasWrap').clientWidth-40);state.scale=Math.min(2.2,available/base.width);$('pdfReaderZoomLabel').textContent=Math.round(state.scale*100)+'%';await renderPage()}
async function toggleBookmark(){try{const active=await ann().toggleBookmark(state.doc.id,state.page);state.bookmarks=await ann().bookmarks(state.doc.id);updateBookmarkButton();setStatus(active?'Página marcada.':'Marcador removido.')}catch(e){handle(e)}}
function updateBookmarkButton(){const b=$('pdfReaderBookmark');if(b)b.classList.toggle('active',state.bookmarks.some(x=>Number(x.page_number)===state.page))}
async function saveProgress(closing){if(!state.doc||!state.total)return;const c=core().getSupabaseClient(),u=await core().getAuthenticatedUser();const elapsed=closing&&state.openedAt?Math.max(0,Math.round((Date.now()-state.openedAt)/1000)):0;const {error}=await c.from('pdf_progress').upsert({user_id:u.id,pdf_id:state.doc.id,current_page:state.page,progress_percentage:Math.round((state.page/state.total)*10000)/100,reading_seconds:Number(state.doc.progress?.reading_seconds||0)+elapsed,last_opened_at:new Date().toISOString(),updated_at:new Date().toISOString()},{onConflict:'user_id,pdf_id'});if(error)throw error}
function toggleSide(){document.getElementById('pdfReaderOverlay')?.classList.toggle('side-collapsed')}
function handle(e){console.error('[PDF Reader]',e);setStatus(e?.message||'Erro no Reader.');alert(e?.message||'Erro no Reader.')}
global.PdfStudyReader=Object.freeze({open,close,prev,next,goto,zoom,fitWidth,toggleBookmark,saveAnnotation,promptNote,createFlashcard,exportToNotes,downloadMarkdown,deleteAnnotation,goToAnnotation,toggleSide});
})(window);
