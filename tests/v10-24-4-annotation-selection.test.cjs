const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const r=p=>fs.readFileSync(p,'utf8');test('v10.24.5 preserva seleção ao clicar em Anotação',()=>{const js=r('public/js/pdf/pdf-reader.js'),pkg=require('../package.json');assert.equal(pkg.version,'10.24.5');assert.match(js,/capturedAt:Date\.now/);assert.match(js,/state\.selected\?\.text/);assert.match(js,/15000/);assert.ok(js.includes('if(text)captureDirectSelection()'));});
