const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const r=p=>fs.readFileSync(p,'utf8');test('v10.24.7 biblioteca atualiza ao abrir a guia sem ativação duplicada',()=>{const js=r('public/js/pdf/pdf-library-ui.js'),pkg=require('../package.json');assert.equal(pkg.version,'10.25.7');assert.ok(js.includes('onTabActivated:activateLibrary'));assert.ok(js.includes('activationPromise=initialize(true)'));assert.ok(!js.includes('window.PdfStudyLibraryUI?.onTabActivated?.()'));assert.ok(js.includes('const sameContext=state.initializedFor===cc'));assert.ok(js.includes('if(sameContext){await load();return;}'));assert.ok(!js.includes('function scheduleLibraryActivationRefresh()'));assert.ok(!js.includes('initialize(true).catch(handle)'));});
