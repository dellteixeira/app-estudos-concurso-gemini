const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const r=p=>fs.readFileSync(p,'utf8');test('v10.24.7 remove data duplicada do cabeçalho e mantém retenção',()=>{const html=r('public/index.html'),pkg=require('../package.json');assert.equal(pkg.version,'10.24.9');assert.doesNotMatch(html,/id="countdownBadge"/);assert.match(html,/id="retentionExamPhase"/);assert.match(html,/Definir data da prova/);});
