const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const r=x=>fs.readFileSync(path.join(__dirname,'..',x),'utf8');
test('serviço de vínculos cria, remove e sincroniza concurso',()=>{const s=r('public/js/pdf/pdf-links.js');for(const t of ["from('pdf_document_links')",'async function create','removeForPdfInConcurso','renameConcursoNow','deleteConcursoNow','processPendingContestOperations'])assert.match(s,new RegExp(t.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')));});
test('migration desacopla colunas contextuais de pdf_documents',()=>{const s=r('supabase/migrations/20260818230000_decouple_pdf_from_concurso.sql');for(const col of ['workspace_id','concurso','materia','assunto'])assert.match(s,new RegExp(`alter table public\\.pdf_documents drop column if exists ${col}`));assert.match(s,/alter table public\.study_workspaces drop column if exists concurso/);});
test('RPC de exclusão completa inclui pdf_document_links',()=>{const s=r('supabase/migrations/20260818230100_extend_delete_my_study_data_for_pdf_links.sql');assert.match(s,/'pdf_document_links'/);assert.match(s,/'pdf_documents'/);});
