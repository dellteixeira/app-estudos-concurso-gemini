name: V10.26 Validated Flashcards Apply

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "One-shot applicator disabled; v10.26.0 is applied by the normal PR branch validation path."
