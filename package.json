{
  "name": "estudo-adaptativo-inteligente",
  "private": true,
  "version": "10.25.7",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "audit": "node scripts/audit-release.mjs",
    "check": "npm run test && npm run audit"
  },
  "engines": {
    "node": ">=20"
  }
}
